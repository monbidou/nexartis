/**
 * ⚠️ FICHIER OBSOLÈTE — NON UTILISÉ EN PRODUCTION
 *
 * Le générateur PDF actif est `lib/pdf-chantier.ts` (jsPDF natif).
 * Ce fichier était un ancien template HTML pour Puppeteer, jamais branché.
 * Le contenu a été retiré pour éviter les confusions (du code orphelin
 * provoquait des erreurs lors d'éditions par méprise).
 *
 * Pour modifier le PDF chantier : éditer `lib/pdf-chantier.ts`.
 */

export {}
